
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Число обусловленности измеряет чувствительность решения линейной задачи к малым возмущениям данных. При плохой обусловленности оценки весов в МНК становятся численно неустойчивыми, а градиентные методы сходятся медленнее.

## Суть

**Число обусловленности** (condition number) матрицы характеризует, насколько сильно малые изменения входных данных (или вычислительные погрешности) могут повлиять на результат решения.

В контексте МНК и аналитической формулы $w^*=(X^\top X)^{-1}X^\top y$ плохая обусловленность делает вычисление весов $w^*$ нестабильным: небольшие возмущения $y$ или $X$ могут приводить к большим изменениям $w$.

$$
\kappa(A)=\|A\|\cdot\|A^{-1}\|
\quad\text{(для обратимой матрицы $A$)}
$$

---

## Детали

### Определение
Для обратимой матрицы $A$ число обусловленности в выбранной норме определяется как
$$
\kappa(A)=\|A\|\cdot\|A^{-1}\|.
$$
В евклидовой (спектральной) норме:
$$
\kappa_2(A)=\frac{\sigma_{\max}(A)}{\sigma_{\min}(A)},
$$
где $\sigma_{\max},\sigma_{\min}$ — максимальное и минимальное сингулярные значения.

Интуиция:
- $\kappa(A)\approx 1$ ⇒ задача хорошо обусловлена (устойчива),
- $\kappa(A)\gg 1$ ⇒ плохо обусловлена (чувствительна к ошибкам).

### Почему это важно в МНК

Аналитическое решение использует обращение $X^\top X$. Если признаки почти линейно зависимы, то $X^\top X$ становится плохо обусловленной.
Тогда:
- вычисление $(X^\top X)^{-1}$ усиливает ошибки округления,
- оценки $w$ становятся нестабильными.

В источнике подчёркивается, что погрешность в $w$ может зависеть от **квадрата числа обусловленности** (в контексте обсуждения проблем точного решения).

### Связь с геометрией и данными

Плохая обусловленность часто возникает, когда:
- признаки сильно коррелированы (мультиколлинеарность),
- масштабы признаков сильно различаются,
- есть «почти дублирующие» признаки (почти линейная зависимость столбцов $X$).

### Влияние на градиентные методы

Если поверхность MSE имеет сильно вытянутые эллипсоиды уровня (что соответствует плохой обусловленности), то градиентный спуск:
- делает «зигзагообразные» шаги,
- сходится медленно,
и требуется аккуратный подбор темпа обучения.

---

## Примеры

1. **Идеальный случай**: $A=I$ ⇒ $\kappa(A)=1$ (максимальная устойчивость).
2. **Диагональная матрица с разными масштабами**: $A=\mathrm{diag}(1,1000)$ ⇒ $\kappa_2(A)=1000$ (сильная чувствительность по одной оси).
3. **Почти вырожденная матрица**: $A=\mathrm{diag}(1,10^{-6})$ ⇒ $\kappa_2(A)=10^6$ (очень плохо обусловлена).
4. **Почти коллинеарные признаки**: второй признак $\approx$ первый ⇒ столбцы $X$ почти линейно зависимы ⇒ $X^\top X$ плохо обусловлена.
5. **Разный масштаб фич**: $x_1$ в рублях (0…10^6), $x_2$ в долях (0…1) ⇒ без масштабирования ухудшается обусловленность.
6. **Влияние на веса**: малое изменение $y$ (шум измерения) при большой $\kappa(X^\top X)$ может дать заметно другой $w$ при почти той же ошибке на обучении.
7. **Сходимость GD**: при вытянутых эллипсоидах уровня MSE шаги направлены поперёк «долины» ⇒ медленная сходимость и чувствительность к $\alpha$.

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **15–18** (обсуждение плохой обусловленности, численной неустойчивости и влияния на сходимость GD)
- Цитата (по смыслу): погрешность оценки $w$ может зависеть от квадрата числа обусловленности, что делает решение неустойчивым.

---

## Вопросы

1. Какие свойства датасета и признаков чаще всего приводят к плохой обусловленности $X^\top X$?
2. Почему почти линейная зависимость признаков делает коэффициенты регрессии «нестабильными»?
3. Как масштабирование признаков может улучшить обусловленность и ускорить градиентный спуск?
4. Почему при плохой обусловленности аналитическое решение может быть хуже итеративного, даже если формально существует?