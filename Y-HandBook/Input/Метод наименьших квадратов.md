
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Метод наименьших квадратов (МНК) — способ обучить линейную регрессию, выбирая веса так, чтобы минимизировать сумму (или среднее) квадратов ошибок, что эквивалентно евклидовой проекции таргетов на подпространство, порождённое признаками.

## Суть

**Метод наименьших квадратов (МНК)** в линейной регрессии — это постановка обучения как задачи минимизации квадратичной ошибки между вектором таргетов $y$ и предсказаниями $Xw$.  
В результате мы ищем такие веса $w$, при которых предсказания являются наилучшим приближением $y$ в смысле евклидовой нормы.

$$
w^*=\arg\min_w \|y-Xw\|_2^2
\qquad\text{(эквивалентно } \arg\min_w \mathrm{MSE}(w)\text{)}
$$

---

## Детали

### Постановка через датасет

Пусть задан датасет $(X,y)$:
- $X\in\mathbb{R}^{N\times D}$ — матрица «объекты–признаки»,
- $y\in\mathbb{R}^N$ — таргеты,
- модель: $\hat y=Xw$ (bias включён через $x_0\equiv1$).

МНК выбирает $w$, минимизирующий квадратичный функционал ошибки.

### Почему «квадраты»

Квадратичная ошибка удобна, потому что:
- приводит к **гладкой** задаче,
- имеет простую геометрию ($L_2$-расстояние),
- даёт аналитическое решение через нормальные уравнения (при выполнении условий).

### Геометрическая интерпретация (ключевая)

Рассмотрим столбцы $X$ как векторы $x^{(1)},\dots,x^{(D)}\in\mathbb{R}^N$ (столбцы признаков). Тогда:
- $Xw$ — линейная комбинация столбцов $X$,
- множество всех $Xw$ — подпространство $\mathrm{span}(x^{(1)},\dots,x^{(D)})\subset\mathbb{R}^N$,
- МНК ищет **проекцию** $y$ на это подпространство.

Критерий оптимальности: остаток $r=y-Xw^*$ ортогонален всем столбцам $X$:
$$
X^\top(y-Xw^*)=0.
$$

### Нормировка (MSE vs SSE)

Минимизация
$$
\|y-Xw\|_2^2
$$
и минимизация
$$
\frac{1}{N}\|y-Xw\|_2^2
$$
дают одно и то же $w^*$ (разница — константный множитель), но MSE удобнее для интерпретации и сравнения качества.

---

## Примеры

1. **Подбор прямой по точкам**: при $D=1$ МНК выбирает $w_1,w_0$, минимизируя сумму квадратов вертикальных отклонений.
2. **Матричная регрессия**: $X$ содержит несколько признаков, МНК подбирает $w\in\mathbb{R}^D$.
3. **Идеальный случай**: если $y$ лежит в $\mathrm{span}(X)$, то существует $w$ с нулевой ошибкой.
4. **Шум**: если $y$ = сигнал + шум, МНК находит лучший линейный сигнал в смысле $L_2$.
5. **Линейная комбинация столбцов**: $Xw=w_1x^{(1)}+\dots+w_Dx^{(D)}$ — приближение $y$ через признаки.
6. **Влияние выбросов**: одна точка с большой ошибкой может «перетянуть» решение из-за квадрата.
7. **Сравнение моделей**: две модели с разными $D$ сравнивают по MSE на тесте, хотя обучались обе через МНК.

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **8–15** (постановка МНК, геометрическое объяснение, условие ортогональности)
- Цитата: «…найти линейную комбинацию столбцов $X$, которая наилучшим способом приближает столбец $y$ по евклидовой норме…» (стр. 13–14)

---

## Вопросы

1. Почему условие $X^\top(y-Xw^*)=0$ выражает «проекционность» решения?
2. В каком смысле МНК приближает $y$ именно в евклидовой геометрии?
3. Почему MSE и $\|y-Xw\|_2^2$ дают одинаковый оптимум по $w$?
4. Как выбросы и мультиколлинеарность влияют на решение МНК?