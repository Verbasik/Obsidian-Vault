
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> В МНК обучение линейной регрессии — это геометрическая проекция вектора таргетов на подпространство, порождённое признаками: ошибка минимальна, когда остаток ортогонален этому подпространству.

## Суть

**Геометрическая интерпретация МНК** утверждает, что оптимальные предсказания $Xw^*$ — это **ортогональная проекция** вектора таргетов $y\in\mathbb{R}^N$ на подпространство $\mathcal{S}=\mathrm{Im}(X)=\mathrm{span}(x^{(1)},\dots,x^{(D)})$, образованное столбцами матрицы признаков $X$.  
Эквивалентное условие оптимальности: остаток $r=y-Xw^*$ ортогонален всем столбцам $X$.

$$
Xw^*=\mathrm{proj}_{\mathrm{Im}(X)}\,y,
\qquad
X^\top(y-Xw^*)=0
$$

---

## Детали

### Пространства и объекты геометрии
- $y\in\mathbb{R}^N$ — вектор наблюдаемых таргетов.
- Столбцы $X$: $x^{(1)},\dots,x^{(D)}\in\mathbb{R}^N$ — векторы значений признаков по всем объектам.
- Множество всех предсказаний линейной модели:
  $$
  \{Xw:\ w\in\mathbb{R}^D\}=\mathrm{Im}(X)\subseteq\mathbb{R}^N.
  $$
Это подпространство: все линейные комбинации столбцов $X$.

### Что минимизируется
МНК минимизирует евклидово расстояние:
$$
\|y-Xw\|_2^2.
$$
Геометрически это значит: найти точку $Xw$ в подпространстве $\mathrm{Im}(X)$, ближайшую к $y$.

### Ортогональность остатка (ключевой критерий)
Пусть
- $\hat y = Xw^*$ — проекция,
- $r=y-\hat y$ — остаток.

Тогда для ортогональной проекции выполняется:
$$
r\perp \mathrm{Im}(X).
$$
Эквивалентно (поскольку столбцы $X$ порождают $\mathrm{Im}(X)$):
$$
\langle x^{(j)}, r\rangle =0\ \ \forall j=1,\dots,D
\quad\Longleftrightarrow\quad
X^\top(y-Xw^*)=0.
$$

### Связь с «нормальными уравнениями»
Условие $X^\top(y-Xw^*)=0$ часто называют нормальными уравнениями:
$$
X^\top X w^* = X^\top y,
$$
что ведёт к аналитическому решению (если $X^\top X$ обратима).

---

## Примеры

1. **$D=1$ (один признак)**: $\mathrm{Im}(X)$ — прямая в $\mathbb{R}^N$; МНК проектирует $y$ на эту прямую.
2. **$D=2$**: $\mathrm{Im}(X)$ — плоскость в $\mathbb{R}^N$ (или подпространство размерности ≤2); предсказание — проекция $y$ на неё.
3. **Идеальная аппроксимация**: если $y\in\mathrm{Im}(X)$, то проекция совпадает с $y$, и ошибка нулевая.
4. **Шум**: $y=\text{сигнал}+\text{шум}$; проекция «отбрасывает» компоненту шума, ортогональную $\mathrm{Im}(X)$.
5. **Ортогональность остатков**: для каждого признака сумма произведений «признак × остаток» равна нулю (векторно: $X^\top r=0$).
6. **Переобучение по признакам**: добавляя признаки, расширяем $\mathrm{Im}(X)$ ⇒ расстояние до $y$ на обучении уменьшается (но не обязательно на тесте).
7. **Коллинеарность**: если столбцы почти линейно зависимы, геометрия «вытягивается» ⇒ вычисления становятся неустойчивыми (связь с обусловленностью).

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **13–14** (проекция на подпространство и условие $X^\top(y-Xw)=0$)
- Цитата: «…найти проекцию вектора $y$ на подпространство, образованное векторами столбцов…» (стр. 13)

---

## Вопросы

1. Почему минимизация $\|y-Xw\|_2^2$ обязательно приводит к ортогональности остатка?
2. Как интерпретировать равенство $X^\top r=0$ на уровне «корреляции» остатка с признаками?
3. Что меняется в геометрии, если признаки линейно зависимы или почти зависимы?
4. Почему расширение пространства признаков гарантированно уменьшает ошибку на обучении, но не гарантирует улучшения на тесте?