
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> MSE — стандартная функция потерь для линейной регрессии: она измеряет среднюю квадратичную ошибку предсказаний и удобна тем, что приводит к выпуклой гладкой задаче оптимизации.

## Суть

**Среднеквадратичная ошибка** (Mean Squared Error, **MSE**) — это функция потерь, равная среднему по объектам квадрату разности между истинным таргетом и предсказанием модели.  

В линейной регрессии минимизация MSE даёт классическую постановку метода наименьших квадратов и допускает как аналитическое решение, так и эффективные итеративные методы.

$$
\mathrm{MSE}(w)=\frac{1}{N}\sum_{i=1}^N\bigl(y_i-\langle x_i,w\rangle\bigr)^2
=\frac{1}{N}\,\|y-Xw\|_2^2
$$

---

## Детали

### Определение

Пусть $X\in\mathbb{R}^{N\times D}$ — матрица признаков, $y\in\mathbb{R}^N$ — таргеты, $\hat y=Xw$ — предсказания. Тогда:
$$
\mathrm{MSE}(w)=\frac{1}{N}\sum_{i=1}^N (y_i-\hat y_i)^2.
$$

### Почему берут среднее, а не сумму

Сумма квадратов ошибок $\|y-Xw\|_2^2$ зависит от размера выборки $N$, поэтому её неудобно сравнивать между выборками разного объёма.  
Нормировка на $N$ делает величину **сопоставимой** и интерпретируемой как «средняя ошибка на объект».

### Свойства, важные для обучения

- **Гладкость**: MSE дифференцируема по $w$ ⇒ доступны градиентные методы.
- **Выпуклость** по $w$ (для линейной модели) ⇒ локальные минимумы отсутствуют, можно стремиться к глобальному минимуму.
- **Геометрический смысл**: минимизация MSE эквивалентна приближению $y$ проекцией на линейную оболочку столбцов $X$ (см. заметки про МНК и геометрию).

### Градиент (для связи с оптимизацией)

Для $L(w)=\mathrm{MSE}(w)$:
$$
\nabla_w L(w)=\frac{2}{N}X^\top(Xw-y).
$$
Это ключ к градиентному спуску и SGD.

---

## Примеры

1. **Мини-пример (3 объекта)**:  
   $y=(1,2,3)$, $\hat y=(1,1,5)$ ⇒ ошибки $e=(0,1,-2)$ ⇒ квадраты $(0,1,4)$ ⇒ $\mathrm{MSE}=(0+1+4)/3=5/3$.
2. **Нулевая MSE**: если $\hat y=y$, то $\mathrm{MSE}=0$.
3. **Связь с $L_2$**: $\mathrm{MSE}=\frac{1}{N}\|e\|_2^2$.
4. **Сравнение выборок**: при одинаковой «средней ошибке» MSE примерно одинакова, даже если одна выборка в 10 раз больше.
5. **Чувствительность к выбросам**: квадрат ошибки усиливает влияние больших промахов (выбросов).
6. **Сдвиг через $x_0\equiv1$**: включив bias в $w$, формула MSE сохраняется с расширенным $X$.
7. **Оптимизация**: минимизация MSE по $w$ — это МНК, можно решать аналитически или градиентно.

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **11–13** (введение MSE, мотивация нормировки, постановка минимизации)
- Цитата: «Функция потерь … называется среднеквадратическим отклонением (Mean Squared Error, MSE).» (стр. 12)

---

## Вопросы

1. Почему нормировка на $N$ важна для интерпретации и сравнения качества моделей?
2. Чем MSE «неудобна» при наличии выбросов и какие альтернативы можно рассмотреть?
3. Почему выпуклость MSE по $w$ критична для гарантий сходимости градиентных методов?
4. Как связаны минимизация MSE и геометрическая проекция на подпространство?