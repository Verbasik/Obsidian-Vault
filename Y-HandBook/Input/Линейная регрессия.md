
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Линейная регрессия — задача предсказания вещественного таргета с помощью линейной модели, обучаемой минимизацией квадратичной ошибки (обычно MSE), что приводит к методу наименьших квадратов.

## Суть

**Линейная регрессия** — это постановка задачи регрессии, в которой зависимость таргета от признаков аппроксимируется линейной функцией параметров $w$ (и, возможно, $w_0$).  
Обучение обычно формулируется как минимизация среднеквадратичной ошибки между предсказаниями и истинными значениями, что даёт классическую задачу **метода наименьших квадратов**.

$$
\hat y_i = f(x_i)=\langle w,x_i\rangle+w_0,\qquad
w^*=\arg\min_w \frac{1}{N}\sum_{i=1}^N (y_i-\hat y_i)^2
$$

---

## Детали

### Постановка задачи

Даны наблюдения $(x_i,y_i)$, где:
- $x_i\in\mathbb{R}^D$ — вектор признаков $i$-го объекта,
- $y_i\in\mathbb{R}$ — вещественный таргет.

Ищем параметры линейной модели, чтобы предсказания $\hat y_i$ максимально хорошо приближали $y_i$ на обучающей выборке.

### Матричная форма

Соберём строки $x_i^\top$ в матрицу $X\in\mathbb{R}^{N\times D}$, таргеты — в $y\in\mathbb{R}^N$. Тогда (при включённом bias через $x_0\equiv 1$):
$$
\hat y = Xw,
\qquad
w^*=\arg\min_w \frac{1}{N}\|y-Xw\|_2^2.
$$

### Почему линейная регрессия удобна

- Простая интерпретация коэффициентов (с оговорками про масштаб и корреляции признаков).
- Гладкая выпуклая задача оптимизации при MSE.
- Доступны как аналитическое решение (через нормальные уравнения), так и итеративные методы (GD/SGD).

### Ограничения

Линейная регрессия аппроксимирует сложные зависимости только через **признаки**: если связь нелинейна, нужно расширять представление (feature engineering). При мультиколлинеарности возможны нестабильные коэффициенты и плохая обусловленность задачи.

---

## Примеры

1. **Прогноз доходности**: $x$ — геологические показатели, $y$ — годовая прибыль шахты.
2. **Одномерная регрессия**: $x$ — площадь квартиры, $y$ — цена; модель $\hat y=w_1x+w_0$.
3. **Многомерная регрессия**: $x=(\text{площадь},\ \text{этаж},\ \text{метро})$.
4. **Сдвиг через $x_0\equiv1$**: добавляем фиктивный признак, чтобы не писать $w_0$ явно.
5. **Нелинейная зависимость через признаки**: добавляем $x^2$ и получаем линейность по $(x,x^2)$.
6. **Переобучение через признаки** (идея): чрезмерное усложнение признаков может ухудшить обобщение.
7. **Влияние выбросов**: из-за квадрата ошибки редкие большие промахи сильно влияют на обучение.

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **8–13** (переход к регрессии, постановка МНК, выбор MSE)
- Цитата: «Мы начнём с использования линейных моделей для решения задачи регрессии… метод наименьших квадратов.» (стр. 8)

---

## Вопросы

1. В чём смысл нормировки MSE на $N$ именно в задаче регрессии?
2. Почему линейная регрессия часто требует инженерии признаков для сложных зависимостей?
3. Когда аналитическое решение МНК становится плохой идеей даже если оно существует?
4. Как связана линейная регрессия с геометрической проекцией в пространстве $\mathbb{R}^N$?