
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Функция потерь количественно измеряет ошибку модели на данных и превращает обучение в задачу оптимизации по параметрам (например, по весам линейной модели).

## Суть

**Функция потерь** (loss function) — это правило, которое сопоставляет параметрам модели число, отражающее качество предсказаний на обучающей выборке: чем меньше значение, тем лучше модель.  
В линейной регрессии естественный выбор — сравнивать вектор предсказаний и вектор таргетов через расстояние (часто через квадрат $L_2$-нормы), что приводит к MSE.

$$
L(w,X,y)\ \text{измеряет, насколько}\ f_w(X)\ \text{близко к}\ y,\quad 
w^*=\arg\min_w L(w,X,y)
$$

---

## Детали

### Формальная постановка (данные и модель)

Пусть задан датасет $(X,y)$:
- $X\in\mathbb{R}^{N\times D}$ — матрица «объекты–признаки», где $i$-я строка $x_i$ — признаки $i$-го объекта,
- $y\in\mathbb{R}^N$ — вектор таргетов,
- линейная модель (без явного свободного члена после добавления $x_0\equiv1$): $f_w(X)=Xw$.

Тогда функция потерь оценивает рассогласование между $Xw$ и $y$.

### Требования к функции потерь

В ML обычно хотят, чтобы лосс:
- был **удобен для оптимизации** (гладкость — плюс, кусочно-постоянные — плохо),
- отражал смысл «ошибки» в задаче,
- давал адекватную интерпретацию на выборках разного размера (поэтому часто берут среднее, а не сумму).

### Потери как функционал

Важно различать:
- **модель** как функцию $f_w$,
- **лосс** как функционал, который каждой модели (каждому $w$) сопоставляет число качества.

Это превращает обучение в задачу:
$$
\min_w L(w;X,y).
$$

### Связь с расстояниями

Для регрессии часто берут лосс как положительную функцию от расстояния между векторами $y$ и $Xw$.

Классический выбор — квадрат евклидовой нормы:
$$
\|y-Xw\|_2^2,
$$
а его нормированная версия даёт MSE (см. отдельную заметку).

---

## Примеры

1. **Абстрактный пример**: модель предсказывает $Xw$, лосс сравнивает с $y$ и выдаёт одно число качества.

2. **Сумма квадратов ошибок (SSE)**:
   $$
   L(w)=\sum_{i=1}^N (y_i-\langle x_i,w\rangle)^2.
   $$
3. **Средняя квадратичная ошибка (MSE)**:
   $$
   \mathrm{MSE}(w)=\frac{1}{N}\sum_{i=1}^N (y_i-\langle x_i,w\rangle)^2.
   $$
4. **Масштаб выборки**: при удвоении $N$ сумма квадратов обычно растёт, а среднее — сравнимо.
5. **Гладкость и оптимизация**: MSE гладкая ⇒ можно применять градиентный спуск.
6. **Классификация (идея)**: лосс должен отражать ошибки классов. Линейная модель + порог требует другого лосса (логистический и т.п.).
7. **Плохой лосс**: кусочно-постоянный по $w$ ⇒ градиенты почти всегда нулевые ⇒ обучение «не едет».

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **10–13** (введение понятия лосса, связь с расстоянием, переход к MSE)
- Цитата: «Функция, оценивающая то, как часто модель ошибается, традиционно называется функцией потерь…» (стр. 10)

---

## Вопросы

1. Почему удобство оптимизации (гладкость) — ключевое требование к функции потерь?
2. Чем принципиально отличается «сумма ошибок» от «средней ошибки» и когда это важно?
3. Можно ли выбирать лосс как расстояние в другом нормированном пространстве (не $L_2$)? Какие последствия?
4. Почему для классификации обычно выбирают другой лосс, хотя модель может быть линейной?