
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Вектор признаков — это численное представление объекта, без которого линейная модель неприменима: модель работает не с «текстом/графом», а с $x\in\mathbb{R}^D$.

## Суть

**Вектор признаков** $x=(x_1,\dots,x_D)\in\mathbb{R}^D$ — это набор **численных** характеристик объекта, используемый моделью для вычисления предсказания.  
Линейные модели называют линейными именно **по этим численным признакам**, поэтому качество и смысл модели критически зависят от выбора и построения признаков.

$$
f(x)=\langle w,x\rangle+w_0,\qquad x\in\mathbb{R}^D
$$

---

## Детали

### Определение

Каждому объекту из множества объектов $X$ сопоставляется вектор $x\in\mathbb{R}^D$, где компоненты $x_i$ — численные признаки (features), описывающие объект в формате, пригодном для обучения и применения модели.

### Требование численности

Линейная модель требует арифметики: умножение на веса, суммирование, скалярное произведение. Поэтому:
- «сырые» объекты (текст, категории, графы) **не подаются напрямую**;
- сначала строится численное отображение (признаковое представление).

### Масштаб и интерпретация

Интерпретация веса $w_i$ зависит от масштаба $x_i$:
- если $x_i$ измеряется в больших единицах, то вес может быть малым (и наоборот);
- сравнивать «важность признаков» по величине весов без учёта масштаба — ошибка.

### Инженерия признаков (feature engineering)

Если зависимость сложная, линейная модель часто «усиливается» **новыми признаками**, являющимися функциями исходных:
- полиномиальные признаки, взаимодействия, логарифмы, индикаторы и т.д.

Формально модель остаётся линейной **по расширенному вектору признаков**.

---

## Примеры

1. **Один признак (объём транзакции)**: $x=(\text{amount})$ — модель строит пороговое правило по сумме.
2. **Два признака**: $x=(\text{amount},\ \text{hour})$ — учитываем величину и время операции.
3. **Признак-сдвиг**: добавляем $x_0\equiv 1$, получаем $x'=(1,x_1,\dots,x_D)$.
4. **Нормализация масштаба**: $x_1$ в рублях и $x_2$ в годах → без масштабирования веса трудно интерпретировать.
5. **Искусственный признак**: исходно $x=(t)$, добавляем $t^2$ → $x'=(t,t^2)$ для кривизны в регрессии.
6. **Взаимодействие**: $x=(a,b)$, добавляем $ab$ → $x'=(a,b,ab)$.
7. **Категория как признак (идея)**: «марка машины» требует кодирования в числа (иначе арифметика бессмысленна).

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **4–6** (требование численных фич и обсуждение категориальных признаков)
- Цитата: «Чтобы применять линейную модель, нужно, чтобы каждый объект уже был представлен вектором численных признаков.» (стр. 4)

---

## Вопросы

1. Почему линейная модель не может работать напрямую с текстом или графом?
2. Какие свойства «хорошего признака» важнее: интерпретируемость, устойчивость, информативность?
3. Как масштабирование признаков влияет на сравнение весов и на численную устойчивость обучения?
4. В каком смысле добавление нелинейных признаков сохраняет «линейность модели»?