
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> $L_2$-норма измеряет «размер» вектора в евклидовой геометрии и позволяет формализовать ошибку регрессии как расстояние между предсказаниями и таргетами.

## Суть

**$L_2$-норма** (евклидова норма) — стандартная мера длины вектора, задающая привычную геометрию в $\mathbb{R}^n$.

В линейной регрессии ошибка модели часто измеряется через $L_2$-норму (обычно в квадрате) разности между вектором предсказаний и вектором истинных таргетов.

$$
\|u\|_2=\sqrt{\sum_{i=1}^n u_i^2},\qquad
\|y-\hat y\|_2=\sqrt{\sum_{i=1}^N (y_i-\hat y_i)^2}
$$

---

## Детали

### Определение

Для вектора $u=(u_1,\dots,u_n)\in\mathbb{R}^n$:
$$
\|u\|_2=\sqrt{u_1^2+\cdots+u_n^2}.
$$
Квадрат нормы:
$$
\|u\|_2^2=\sum_{i=1}^n u_i^2.
$$

### Почему часто используют квадрат нормы

В задачах оптимизации удобнее минимизировать $\|u\|_2^2$, потому что:
- исчезает корень (проще аналитика и градиенты),
- функция становится гладкой и выпуклой по параметрам в линейных моделях.

### Роль в линейной регрессии

Пусть предсказания $\hat y=Xw$. Тогда **вектор ошибки** $e=y-\hat y=y-Xw$, и типичный лосс:
$$
L(w)=\|y-Xw\|_2^2
\quad\text{или}\quad
\mathrm{MSE}(w)=\frac{1}{N}\|y-Xw\|_2^2.
$$
Геометрически это означает приближение $y$ в смысле евклидовой геометрии (минимизация расстояния).

---

## Примеры

1. **Длина вектора**: $u=(3,4)$ ⇒ $\|u\|_2=\sqrt{3^2+4^2}=5$.
2. **Квадрат нормы**: $u=(3,4)$ ⇒ $\|u\|_2^2=25$.
3. **Ошибка на 3 объектах**: $y=(1,2,3)$, $\hat y=(1,1,5)$ ⇒ $e=(0,1,-2)$, $\|e\|_2=\sqrt{0+1+4}=\sqrt5$.
4. **SSE (сумма квадратов ошибок)**: для $e=(0,1,-2)$ получаем $\|e\|_2^2=5$.
5. **MSE**: при $N=3$ и $\|e\|_2^2=5$ ⇒ $\mathrm{MSE}=5/3$.
6. **Сравнение выборок разного размера**: SSE растёт с $N$, поэтому MSE удобнее для сравнения качества.
7. **Линейная регрессия как приближение**: минимизация $\|y-Xw\|_2^2$ ищет «наилучшее» приближение $y$ линейной комбинацией столбцов $X$.

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **10–12** (выбор $L_2$-нормы для ошибки и переход к MSE)
- Цитата: «$L_2$-норма разницы — это евклидово расстояние…» (стр. 11)

---

## Вопросы

1. Почему минимизация $\|e\|_2^2$ обычно предпочтительнее минимизации $\|e\|_2$?
2. Что меняется в решении, если вместо $L_2$ использовать другую норму (например, $L_1$)?
3. Почему для сравнения качества на выборках разного размера удобнее MSE, а не $\|e\|_2^2$?
4. Как геометрически интерпретировать минимизацию $\|y-Xw\|_2^2$?

---

## Полезные источники

- [What is Norm in Machine Learning?](https://www.youtube.com/watch?v=FiSy6zWDfiA)
- [Vector Norms](https://www.youtube.com/watch?v=DdySKZ9LQ7Q)
