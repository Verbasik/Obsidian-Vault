
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Линейная модель в ML описывает таргет как взвешенную сумму численных признаков (плюс сдвиг) и служит базовым строительным блоком для регрессии и классификации.

## Суть

**Линейная модель** — это параметризованное семейство функций, линейных по численным признакам объекта: предсказание строится как скалярное произведение признаков на веса и добавление свободного члена.  
Она удобна тем, что сводит обучение к поиску оптимального вектора параметров и часто обладает хорошей интерпретируемостью.

$$
f(x)=\langle w,x\rangle+w_0
$$

---

## Детали

### Определение

Пусть объект описан вектором признаков $x=(x_1,\dots,x_D)\in\mathbb{R}^D$. Линейная модель задаётся параметрами $w\in\mathbb{R}^D$ и $w_0\in\mathbb{R}$ и вычисляет предсказание $f(x)$ по формуле выше.

### Интерпретация параметров

- $w_i$ показывает вклад $i$-го признака: при фиксированных прочих признаках рост $x_i$ изменяет предсказание пропорционально $w_i$.
- $w_0$ — **сдвиг (bias)**. Его часто поглощают, добавляя фиктивный признак $x_0\equiv 1$.

### Геометрический смысл

Для классификации линейная модель задаёт **разделяющую гиперплоскость**:
$$
\langle w,x\rangle+w_0=0,
$$
которая делит пространство признаков на два полупространства.

---

## Примеры

1. **Регрессия**: $D=1$**: $w=2,\ w_0=1,\ x=3 \Rightarrow f(x)=2\cdot 3+1=7$.
2. **Регрессия**: $D=2$**: $w=(1,-4),\ w_0=2,\ x=(3,1)\Rightarrow f=1\cdot3-4\cdot1+2=1$.
3. **Сдвиг как признак**: $x'=(1,x_1,x_2)$, $w'=(w_0,w_1,w_2)$ ⇒ $f(x)=\langle w',x'\rangle$.
4. **Классификация порогом (знак)**: предсказываем класс 1, если $f(x)>0$, иначе 0.
5. **Порог по одной фиче**: $f(x)=x-100$ ⇒ транзакции с $x>100$ попадают в «подозрительные».
6. **Гиперплоскость в $\mathbb{R}^2$**: $w=(1,1), w_0=-3$ ⇒ граница $x_1+x_2=3$.
7. **Влияние масштаба**: если заменить $x_1$ на $10x_1$, то для сохранения смысла веса изменятся (важно при интерпретации $w_i$).

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **3–5** (введение линейной модели и интерпретация весов)
- Цитата: «Модель называют линейной, если она является линейной по этим численным признакам.» (стр. 4)

---

## Вопросы

1. Почему линейность определяется **по признакам**, а не по исходному “объекту” (текст/граф/изображение)?
2. Как изменится интерпретация весов при сильной корреляции признаков?
3. Почему добавление признака $x_0\equiv1$ эквивалентно свободному члену $w_0$?
4. В каких случаях линейная разделяющая гиперплоскость принципиально недостаточна?