
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Ортогональная проекция переводит вектор в ближайший в евклидовой метрике элемент подпространства. В МНК предсказание — это проекция таргетов на подпространство, порождённое признаками.

## Суть

**Ортогональная проекция** в евклидовом пространстве — это отображение, которое каждому вектору $y$ сопоставляет вектор $\hat y$ из подпространства $\mathcal{S}$ так, что расстояние $\|y-\hat y\|_2$ минимально.  
Критерий проекции: разность (остаток) $r=y-\hat y$ ортогональна подпространству $\mathcal{S}$.

$$
\hat y=\mathrm{proj}_{\mathcal{S}}(y)
=\arg\min_{s\in\mathcal{S}}\|y-s\|_2,
\qquad
y-\hat y\ \perp\ \mathcal{S}
$$

---

## Детали

### Определение (через минимизацию расстояния)
Пусть $\mathcal{S}\subseteq\mathbb{R}^N$ — линейное подпространство. Тогда проекция $y$ на $\mathcal{S}$ — это:
$$
\hat y=\arg\min_{s\in\mathcal{S}}\|y-s\|_2.
$$
Такой $\hat y$ единственен (в евклидовой геометрии).

### Эквивалентный критерий (ортогональность остатка)
Обозначим $r=y-\hat y$. Тогда:
$$
\langle r, s\rangle=0\ \ \forall s\in\mathcal{S}.
$$
То есть $r\in\mathcal{S}^\perp$.

### Проекция в МНК
В линейной регрессии:
- $y\in\mathbb{R}^N$,
- $\mathcal{S}=\mathrm{Im}(X)=\{Xw:\ w\in\mathbb{R}^D\}$.

Тогда МНК выбирает $\hat y=Xw^*$ как:
$$
Xw^*=\mathrm{proj}_{\mathrm{Im}(X)}(y).
$$
Ортогональность остатка к $\mathrm{Im}(X)$ эквивалентна системе:
$$
X^\top(y-Xw^*)=0.
$$

### Когда проекция «ломается» вычислительно
Геометрическое понятие остаётся корректным, но вычисление может стать нестабильным, если базис подпространства «плохой» (почти линейная зависимость столбцов $X$), что связано с числом обусловленности $X^\top X$.

---

## Примеры

1. **Проекция на прямую в $\mathbb{R}^2$**: $\mathcal{S}=\mathrm{span}(v)$; $\hat y$ — ближайшая точка на прямой.
2. **Проекция на плоскость в $\mathbb{R}^3$**: $\mathcal{S}$ — плоскость через 0; $\hat y$ — «перпендикулярный сброс».
3. **Разложение**: $y=\hat y + r$, где $\hat y\in\mathcal{S}$, $r\in\mathcal{S}^\perp$.
4. **Идеальный случай МНК**: если $y\in\mathrm{Im}(X)$, то $\hat y=y$ и $r=0$.
5. **Шумовая компонента**: $y=s+\epsilon$, где $\epsilon\perp\mathcal{S}$ ⇒ проекция восстанавливает $s$.
6. **Избыточные признаки**: добавление признаков расширяет $\mathcal{S}$ ⇒ расстояние до $y$ не увеличивается.
7. **Почти коллинеарные признаки**: геометрически $\mathcal{S}$ почти та же, но вычислительно коэффициенты (представление $\hat y=Xw$) могут стать неустойчивыми.

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **13–14** (проекция $y$ на подпространство столбцов $X$ и условие ортогональности)
- Цитата: «…найти проекцию вектора $y$ на подпространство, образованное векторами…» (стр. 13)

---

## Инфографика

![[МНК-геометрический подход.pdf]]

---

## Вопросы

1. Почему определение проекции через минимум расстояния эквивалентно условию ортогональности остатка?
2. Как из условия $r\perp\mathrm{Im}(X)$ получается матричная запись $X^\top r=0$?
3. Может ли проекция быть неединственной? В каких геометриях/нормах это возможно?
4. Почему плохая обусловленность влияет на устойчивость вычисления проекции через коэффициенты $w$?