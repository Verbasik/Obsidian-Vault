
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Вектор весов — это параметры линейной модели, которые задают вклад каждого признака в предсказание и подбираются обучением (минимизацией функции потерь).

## Суть

**Вектор весов** $w=(w_1,\dots,w_D)\in\mathbb{R}^D$ — набор параметров линейной модели, определяющий, как каждый признак $x_i$ влияет на предсказание.  
Обучение линейной модели сводится к поиску $w$ (и, при наличии, $w_0$), которые минимизируют ошибку на данных.

$$
f(x)=\langle w,x\rangle+w_0=\sum_{i=1}^D w_i x_i+w_0
$$

---

## Детали

### Определение и роль параметров

- $w_i$ — коэффициент (вес) при $i$-м признаке $x_i$.
- При фиксированных прочих признаках изменение $x_i$ на $\Delta$ изменяет предсказание на $w_i\Delta$.
- $w_0$ — свободный член (bias), часто включают в $w$ через фиктивный признак $x_0\equiv 1$.

### Интерпретация знака и величины весов

- Если $w_i>0$, рост $x_i$ **увеличивает** предсказание (в регрессии) или «сдвигает» решение в сторону одного класса (в классификации после порога).
- Если $w_i<0$, рост $x_i$ **уменьшает** предсказание.
- Величина $|w_i|$ нельзя напрямую трактовать как «важность признака» без учёта **масштаба** $x_i$.

### Ограничения интерпретации

Интерпретация весов может «ломаться» в типичных ситуациях:
- **Приближённая линейная зависимость признаков** (мультиколлинеарность): веса могут стать нестабильными и потерять физический смысл.
- **Смена обучающей выборки**: веса могут существенно меняться на малых данных, хотя при росте данных ожидается стабилизация.

### Геометрический смысл

- В регрессии $w$ задаёт направление, вдоль которого растёт предсказание.
- В классификации гиперплоскость $\langle w,x\rangle+w_0=0$ имеет нормаль $w$: именно она определяет ориентацию границы.

---

## Примеры

1. **Одномерная регрессия**: $w=2,\ w_0=1$. При росте $x$ на 1 предсказание растёт на 2.
2. **Два признака**: $w=(1,-4),\ w_0=2$. Признак $x_1$ увеличивает ответ, $x_2$ уменьшает.
3. **Сдвиг через фиктивный признак**: $x'=(1,x_1,\dots,x_D)$, $w'=(w_0,w_1,\dots,w_D)$.
4. **Пороговая классификация**: $f(x)=0$ — граница; знак $f(x)$ задаёт класс.
5. **Смена масштаба**: если заменить $x_1$ на $10x_1$, то для «того же смысла» нужно заменить $w_1$ на $w_1/10$.
6. **Коррелированные признаки**: при $x_2\approx 2x_1$ веса могут стать большими по модулю с противоположными знаками, компенсируя друг друга.
7. **Нестабильность на малых данных**: добавление/удаление нескольких точек может заметно изменить $w$ при той же визуальной зависимости.

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **3–7** (веса как параметры, интерпретация, предостережения)
- Цитата: «Вектор $w$ часто называют вектором весов, так как на предсказание модели можно смотреть как на взвешенную сумму признаков…» (стр. 3–4)

---

## Вопросы

1. Почему нельзя считать «малый вес ⇒ признак не важен» без анализа масштаба?
2. Как мультиколлинеарность приводит к потере смысла коэффициентов $w_i$?
3. Как геометрически интерпретировать $w$ в задачах классификации?
4. Почему веса могут меняться при смене обучающей выборки и что это означает для доверия к интерпретации?