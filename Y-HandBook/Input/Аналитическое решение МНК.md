
> [!ABSTRACT] 💡 ОСНОВНАЯ ИДЕЯ
> Аналитическое решение МНК выражает оптимальные веса через матрицы $X^\top X$ и $X^\top y$; оно быстро даёт точный ответ на малых задачах, но требует обратимости и может быть вычислительно дорогим и численно нестабильным.

## Суть

**Аналитическое решение МНК** получается из условия ортогональности остатка $y-Xw$ столбцам $X$ и приводит к нормальным уравнениям.  
При обратимости матрицы $X^\top X$ оптимальные веса выражаются формулой:

$$
X^\top (y - Xw^*) = 0
\quad\Longrightarrow\quad
w^* = (X^\top X)^{-1}X^\top y.
$$

---

## Детали

### Вывод через нормальные уравнения
МНК минимизирует
$$
\|y-Xw\|_2^2.
$$
Оптимальность эквивалентна ортогональности остатка $r=y-Xw^*$:
$$
X^\top r = X^\top(y-Xw^*)=0,
$$
то есть
$$
X^\top X w^* = X^\top y.
$$
Если $X^\top X$ обратима, то
$$
w^*=(X^\top X)^{-1}X^\top y.
$$

### Условие существования именно такой формулы
Формула с обратной матрицей требует, чтобы:
- $X^\top X$ была **невырождена** (обратима),
что обычно связано с тем, что признаки не должны быть линейно зависимыми (по крайней мере, точно).

Если же обратимости нет, задача всё равно имеет решения МНК, но выражение требует обобщений (например, псевдообратной) — это выходит за рамки данного фрагмента.

### Вычислительная сложность (из источника)
При прямом использовании формулы появляется необходимость обращения матрицы размера $D\times D$. В источнике подчёркивается оценка сложности порядка:
- перемножение для $X^\top X$: $O(ND^2)$,
- обращение: $O(D^3)$,
и суммарно (как приводится в тексте) — $O(D^2N + D^3)$ при типичной организации вычислений.

### Практические замечания
- Не стоит явно строить и хранить $X^\top X$, если можно организовать вычисления аккуратнее (в тексте отмечается, что порядок умножений важен).
- Даже когда обратимость формально есть, матрица может быть **плохо обусловлена**, что делает решение численно неустойчивым (подводит к теме обусловленности и регуляризации/итеративных методов).

---

## Примеры

1. **Малое число признаков**: $D$ малое, $N$ умеренное ⇒ аналитическое решение удобно и быстро.
2. **Полный ранг**: признаки линейно независимы ⇒ $X^\top X$ обратима ⇒ формула применима напрямую.
3. **Линейная зависимость признаков**: один признак = сумма других ⇒ $X^\top X$ вырождается ⇒ формула с обратной матрицей неприменима.
4. **Почти зависимые признаки**: $X^\top X$ обратима, но плохо обусловлена ⇒ веса могут резко меняться при малых изменениях данных.
5. **Большие данные**: $N$ очень велико ⇒ построение $X^\top X$ становится тяжёлым ⇒ предпочитают итеративные методы.
6. **Порядок вычислений**: вычислить $X^\top y$, затем умножить на $(X^\top X)^{-1}$ — обычно выгоднее, чем строить промежуточные большие объекты.
7. **Сравнение с GD/SGD**: аналитика даёт решение «за один раз», но требует кубических операций по $D$; GD/SGD — итеративно, но масштабируется.

---

## Источник

- Документ: [Y-HanbBook](https://education.yandex.ru/handbook/ml)
- Раздел/страницы: 2.2, стр. **14–15** (вывод $w^*=(X^\top X)^{-1}X^\top y$, обсуждение обратимости и сложности)
- Цитата: «…из уравнения $X^\top(y-Xw)=0$ уже очень легко выразить $w$: $w=(X^\top X)^{-1}X^\top y$.» (стр. 14–15)

---

## Вопросы

1. Какое свойство данных/признаков соответствует обратимости $X^\top X$?
2. Почему плохая обусловленность приводит к численной неустойчивости оценок $w$?
3. В каких режимах ($N\gg D$ или $D\gg N$) аналитическое решение особенно неудобно?
4. Какие альтернативы существуют, если $X^\top X$ не обратима?