## Суть

**Область значений функции** — это множество всех значений, которые функция фактически принимает на своей области определения. Она является подмножеством области прибытия и определяется действием функции на аргументы.

Если $f : X \to Y$, то область значений обозначается $f(X)$ и всегда выполняется $f(X) \subset Y$.

---

## Детали

### Формальное определение

Пусть задана функция
$$
f : X \to Y.
$$
**Областью значений** функции называется множество
$$
f(X) := \{\, y \in Y \mid \exists x \in X : y = f(x) \,\}.
$$

Элементы множества $f(X)$ называются **значениями функции**.

### Отличие от области прибытия

Важно различать:
- **область прибытия** $Y$ — множество, в которое отображает функция по определению;
- **область значений** $f(X)$ — множество значений, которые реально достигаются.

Вообще говоря,
$$
f(X) \neq Y.
$$

Совпадение $f(X)=Y$ имеет место тогда и только тогда, когда функция является **сюръективной**.

## 🔁 Сюръективная функция (сюръекция)

**Сюръективная функция** — это отображение  $( f : X \rightarrow Y )$,  при котором **каждый элемент множества (Y)** имеет **хотя бы один прообраз** в множестве (X).

Формально:

$$
\forall y \in Y \;\; \exists x \in X : f(x) = y
$$

### 💡 Интуитивное объяснение

Говорят, что функция **«накрывает» всё множество \(Y\)**:  
в области значений **нет ни одного элемента, который остался бы без пары**.

Иначе говоря, **область значений функции совпадает с множеством \(Y\)**.

### Зависимость от области определения

Область значений зависит не только от формулы функции, но и от её области определения. При изменении области определения область значений, как правило, также изменяется.

---

## Примеры

### Пример 1. Квадратичная функция  
$$
f(x)=x^2,\quad X=\mathbb R
$$
$$
f(X)=[0,+\infty)
$$

### Пример 2. Та же формула, другая область определения  
$$
f(x)=x^2,\quad X=[-1,2]
$$
$$
f(X)=[0,4]
$$

### Пример 3. Тригонометрическая функция  
$$
f(x)=\sin x,\quad X=\mathbb R
$$
$$
f(X)=[-1,1]
$$

### Пример 4. Линейная функция  
$$
f(x)=2x+1,\quad X=\mathbb R
$$
$$
f(X)=\mathbb R
$$

### Пример 5. Постоянная функция  
$$
f(x)=c,\quad X\neq\varnothing
$$
$$
f(X)=\{c\}
$$

### Пример 6. Проекция  
$$
\operatorname{pr}_1 : X_1 \times X_2 \to X_1
$$
$$
\operatorname{pr}_1(X_1\times X_2)=X_1
$$

---

## Источник

- Документ: [[Математический анализ.pdf]]
- Раздел: §3 «Функция», стр. 34
- Ключевая цитата:  
  > «Множество всех значений функции, которые она принимает на элементах множества $X$, будем называть множеством значений функции.»

---

## Вопросы

- Может ли область значений совпадать с областью прибытия?
- Как область значений меняется при сужении области определения?
- Может ли область значений быть пустой?
- Как связана область значений с понятием сюръективности?